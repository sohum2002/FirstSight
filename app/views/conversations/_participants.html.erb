
<table>
		<tr>
			<% conversation.participants.each do |participant| %>
			  <% unless participant == current_user %>
				  	<td><p><%= participant %></p></td>
				  	<td><%= image_tag participant.image(:thumb) %></td>
				</span>
			  <% end %>
			<% end %>
		</tr>
</table>